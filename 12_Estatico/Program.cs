class Program
{
    static void Main(string[] args)
    {
        // Método
        Exemplo.soma(10, 50);

        // Atributo
        Console.WriteLine($"Bem-Vindo, {Exemplo.nome}");
    }
}